

### (1) 可做性强的基础子项目（建议优先完成）

这些项目是实现你们“双谱分离”目标的基础，同时解决了往届项目遗留的关键问题，可做性强，成果显著。

**子项目A：自动化多通道数据采集系统的搭建**

* [cite_start]**目标**：解决往届项目[cite: 288, 464]手动读取万用表导致的数据量少、频率低、无法同步的问题。这是你提到的“自动化测量”的第一步。
* **内容**：
    1.  **硬件**：使用Arduino或ESP32作为主控。
    2.  [cite_start]**信号采集**：热电芯片的电压信号非常微弱（mV级别），需要先放大。可使用你们方案中提到的INA放大器 [cite: 63]，或者更简单地使用**ADS1115**（一个16位ADC模块，自带可编程放大器PGA），它可以同时采集4路差分信号。
    3.  **数据记录**：将读取到的电压值、时间戳通过串口实时发送到电脑，用Python脚本（如`pyserial`库）接收并保存为`.csv`文件，为后续CNN训练准备数据集。
* **价值**：这是实现一切高级创新的**基石**。

**子项目B：冷端稳定与风噪抑制装置的验证**

* [cite_start]**目标**：解决2024秋季组发现的“冷端被日照加热” [cite: 287, 100] [cite_start]和2022秋季组担心的“风致漂移” [cite: 482, 100] 问题。
* **内容**：
    1.  [cite_start]**冷端散热**：在热电芯片的冷端（背面）涂抹导热硅脂，紧密贴装一个**大型CPU散热鳍片** [cite: 62]。
    2.  [cite_start]**冷端遮阳**：设计一个“反射罩”（如用硬纸板+铝箔） [cite: 62, 100]，保护散热鳍片不被太阳直射，只允许聚光镜反射的光束照射到热端（正面）。
    3.  [cite_start]**风噪抑制**：借鉴2022秋季组的“差分测量”思想 [cite: 419, 492]。在聚光焦点处放置**两块**完全相同的芯片（比如都涂黑）。
        * 芯片1（信号片）：放置在焦点中心，测量（辐射+风）。
        * 芯片2（参考片）：放置在焦点旁（不受聚光照射），测量（环境+风）。
        * [cite_start]**V_signal = V_芯片1 - V_芯片2**。这个差分信号理论上可以消除共模的风噪和环境温度漂移。你们方案中也提到了这个思路 [cite: 37, 66]。
* **价值**：解决往届项目的核心痛点，是获取稳定、可信数据的**前提**。

**子项目C：“双灶-双谱”分离系统的实现（替代滤波轮）**

* [cite_start]**目标**：实现你们“双谱分离” [cite: 10] [cite_start]的核心创意，替代复杂的滤波轮 [cite: 34]。
* **内容**：
    1.  [cite_start]搭建**两个完全相同**的聚光装置（可以使用2024秋季组改进的六边形平面镜阵列 [cite: 191, 27]）。
    2.  [cite_start]**装置A（总辐射通道 SW+LW）**：在焦点处放置一块涂黑的热电芯片（像2024组那样 [cite: 260]）。它吸收所有波段的辐射。
    3.  [cite_start]**装置B（长波通道 LW）**：在焦点处放置一块**相同**的涂黑芯片，但在这块芯片**前方**加一个“短波滤光片”（即只让长波红外通过 [cite: 35]）。
        * *昂贵方案*：使用锗(Ge)或硅(Si)窗口片。
        * [cite_start]*可行方案*：使用你们方案中提到的IR-cut/玻璃片 [cite: 74] [cite_start]是**错误**的，那是SW窗。你们需要LW窗 [cite: 72]。一个廉价的LW窗材料是**不透光的薄黑色聚乙烯塑料膜**（如黑色垃圾袋），它能阻挡可见光（SW），但对中远红外（LW）透明。
    4.  [cite_start]**数据处理**：通过自动化系统 [cite: 63] 同时读取 $V_A$ 和 $V_B$。
        * $V_A \approx k \cdot (P_{SW} + P_{LW})$
        * $V_B \approx k \cdot (P_{LW})$
        * 计算得出：$V_{SW\_only} = V_A - V_B$
* **价值**：用非常可行的方法，在物理上真正分离了太阳短波辐射和大气长波辐射。

---

### (2) 更具创新性的子项目（在(1)的基础上）

[cite_start]在你提到的“CNN训练”思路 [cite: 104] 基础上，可以衍生出以下极具创新性的工作：

**创新项目一：基于CNN的“天空图像-辐射分量”预测模型**

* **目标**：这完全是你提到的CNN思路的实现。训练一个模型，让它“看一眼”天空，就能预测出对应的短波(SW)和长波(LW)辐射值。
* **内容**：
    1.  [cite_start]**数据采集（Sensor Fusion）**：在“子项目C”的双灶系统旁，架设一个摄像头（如USB摄像头或树莓派摄像头），**同步**拍摄聚光灶朝向的天空照片 [cite: 269]。
    2.  **数据集**：自动化采集数据（比如连续采集数天）。你将得到一个包含（`时间戳`, `天空图像.jpg`, `V_SW`, `V_LW`）的大型数据集。
    3.  **模型训练**：
        * **输入 (X)**：天空图像
        * **输出 (Y)**：一个包含2个值的向量 `[V_SW, V_LW]`
        * **模型**：使用一个预训练好的CNN（如 MobileNetV2 或 ResNet）作为图像特征提取器，去掉最后一层，接上一个有2个输出神经元的`Dense`（全连接）层。
* [cite_start]**价值**：这超越了2024组的RGB直方图分析 [cite: 270][cite_start]，也超越了2022组的像素点百分比计算 [cite: 466]，是真正意义上的“智能”天气监测。

**创新项目二：基于时间序列模型（CNN/LSTM）的云量/辐射短期预报**

* **目标**：利用你们采集到的高频时间序列数据，预测未来几分钟的天气变化（例如云会不会飘过来）。
* **内容**：
    1.  **数据**：使用“子项目C”采集到的高频（例如每秒1次）`V_SW`和`V_LW`时间序列数据。
    2.  **模型训练**：
        * **输入 (X)**：过去60秒的 `[V_SW, V_LW]` 数据（一个 60x2 的矩阵）。
        * **输出 (Y)**：未来15秒的 `V_SW` 值。
        * **模型**：使用1D-CNN（一维卷积）或更适合时间序列的LSTM/GRU网络。
* **价值**：当 $V_{SW}$ 骤降而 $V_{LW}$ 骤升时，说明云来了。你们的模型可以实现“太阳能发电波动预警”或“体感温度变化预警”，应用价值极高。

**创新项目三：“全光谱”分离观测阵列**

* [cite_start]**目标**：将2024组的“彩色光谱分析” [cite: 260] [cite_start]和你们的“双谱分离” [cite: 10] 思想进行终极融合。
* **内容**：
    1.  [cite_start]**装置A（短波光谱SW）**：在聚光焦点处，不再是放1块黑芯片，而是放一个**4芯阵列**（像2024组那样 [cite: 260][cite_start]）：**黑、红、绿、蓝**。同时，在整个阵列前加装**SW窗**（如薄玻璃片 [cite: 74]，阻断LW）。
    2.  [cite_start]**装置B（长波总辐射LW）**：保持不变，1块黑芯片+**LW窗**（如黑色塑料膜 [cite: 72]）。
* **价值**：你们将得到史无前例的数据：
    * 来自装置A：天空的**短波光谱成分**（比如天是“深蓝”还是“灰白”，太阳是“金黄”还是“惨白”）。
    * 来自装置B：天空的**长波热辐射**（比如云层是“热”还是“冷”）。
    * 这套系统可以同时分析**瑞利散射**（蓝天）、**米氏散射**（雾霾/云）和**大气逆辐射**（热量），是真正意义上的“天空辐射观测仪”。